{
  // Editor / language-server default project: everything the tests and
  // tooling project sees. CI gates use tsconfig.build.json (shipped source),
  // tsconfig.test.json (tests + tooling) and tsconfig.preview.json (apps/).
  "extends": "./tsconfig.test.json"
}
